services:
  - type: web
    name: platefeorme-commercial
    runtime: node
    region: frankfurt
    plan: free
    
    buildCommand: npm install && npm run build
    startCommand: npm start
    
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXTAUTH_URL
        fromDatabase:
          name: main_db
          property: render_url
      - key: NEXTAUTH_SECRET
        generateValue: true
